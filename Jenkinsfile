pipeline {
    agent {
        label 'zxp_node1'
    }
    stages {
        stage('Build') {
            steps {
                sh 'mvn package';
            }
            //hhhh
            //test
        }
    }
}
